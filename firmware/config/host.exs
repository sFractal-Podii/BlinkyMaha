import Config

# Add configuration that is only needed when running on the host here.

##only here for local testing purposes 
config :firmware, 
    client_id: ":sfractal2020",
    mqtt_host: "127.0.0.1",
    mqtt_port: 1883,
    user_name: "plug",
    password: "fest"